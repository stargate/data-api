package io.stargate.sgv2.jsonapi.service.operation.query;

/**
 * Marker interface for interfaces that are used to build a query using the Java Driver query builder.
 * <p>
 * Implementations of this interface are used to do things like add the WHERE clause to a query, see
 * implementations for more details.
 * <p>
 * Here has a marker interface to make it easier to find all the classes that are used to build a query.
 */
public interface CQLClause {
}
