package io.stargate.sgv2.jsonapi.api.model.command.table.definition.indexes;

/**
 * Describes an index, from the user perspective.
 *
 * <p>All descriptions of schema etc the user gives use are called `Desc` classes. This is just a
 * marker for now more work needed to see what is common between them, trying not to break thigns
 * for now.
 */
public interface IndexDesc {}
