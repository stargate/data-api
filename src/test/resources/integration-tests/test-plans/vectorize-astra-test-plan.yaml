name: Astra ENV
customTarget:
  name: ${TARGET_NAME}
  backend: astra
  connection:
    domain: ${ENDPOINT}
    port: 443
    basePath: /api/json/v1
workflows:
  - vectorize-header-workflow
  - vectorize-shared-workflow
ignoreDisabled: true